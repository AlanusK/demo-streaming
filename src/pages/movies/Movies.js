import Content from '../../components/content';

const Movies = () => {
    const title = 'Popular Movies'
    const programType = 'movie'
    return <Content title={title} programType={programType} />  
}

export default Movies;